apiVersion: v1
kind: Service
metadata:
  labels:
    app: buildkit
  name: buildkit
  namespace: buildkit
spec:
  type: ClusterIP
  ports:
    - port: 1234
      protocol: TCP
  selector:
    app: buildkit
