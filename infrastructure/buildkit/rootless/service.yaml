apiVersion: v1
kind: Service
metadata:
  labels:
    app: buildkitd
  name: buildkitd
  namespace: buildkit
spec:
  type: ClusterIP
  ports:
    - port: 1234
      protocol: TCP
  selector:
    app: buildkitd
