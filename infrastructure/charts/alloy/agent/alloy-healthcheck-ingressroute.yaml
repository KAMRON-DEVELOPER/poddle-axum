apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: alloy-agent-healthcheck-ingressroute
  namespace: alloy-agent
spec:
  entryPoints: []
    # If not specified, HTTP routers will accept requests from all EntryPoints in the list of default EntryPoints.
    # - web
    # - websecure
  routes:
    - match: Host(`alloy-agent.poddle.uz`)
      kind: Rule
      services:
        - name: alloy-agent
          port: 12345
  # tls:
  #   secretName: wildcard-poddle-uz-tls-secret
