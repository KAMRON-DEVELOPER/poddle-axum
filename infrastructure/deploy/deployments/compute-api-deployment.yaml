apiVersion: apps/v1
kind: Deployment
metadata:
  name: compute-api
  namespace: poddle-system
  labels:
    service: compute-api
spec:
  replicas: 1
  selector:
    matchLabels:
      service: compute-api
  template:
    metadata:
      labels:
        service: compute-api
    spec:
      containers:
        - name: compute-api
          image: kamronbekdev/poddle-compute-api:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: service-config
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
