apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: poddle-system
  labels:
    service: users-api
spec:
  replicas: 1
  selector:
    matchLabels:
      service: users-api
  template:
    metadata:
      labels:
        service: users-api
    spec:
      volumes:
        - name: config-volume
          configMap:
            name: service-config
            items:
              - key: config.json
                path: config.json
      containers:
        - name: users-api
          image: kamronbekdev/poddle-users-api:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          # env:
          #   - name: CONFIG
          #     value: /etc/poddle/config.json
          # volumeMounts:
          #   - name: config-volume
          #     mountPath: /etc/poddle
          #     readOnly: true
          volumeMounts:
            - name: config-volume
              mountPath: /app/services/users-api
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
